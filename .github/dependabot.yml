version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    ignore:
      - dependency-name: "@ardurai/contracts"
        # git-pinned; update manually to control breaking changes
